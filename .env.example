# === Database Configuration ===
# Pilih 'sqlite' atau 'mysql'
DB_TYPE=sqlite

# SQLite (hanya jika DB_TYPE=sqlite)
# DB_FILE=./nvr.db

# MySQL (hanya jika DB_TYPE=mysql)
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=nvr

# === Security ===
# WAJIB diganti untuk production!
SESSION_SECRET=ganti-dengan-string-acak-yang-panjang
DEFAULT_ADMIN_PASSWORD=ganti-dengan-password-yang-kuat

# === CORS Whitelist ===
# Domain yang diizinkan, pisahkan dengan koma
# Gunakan * untuk wildcard subdomain, contoh: *.example.com
CORS_WHITELIST=localhost,*.example.com
